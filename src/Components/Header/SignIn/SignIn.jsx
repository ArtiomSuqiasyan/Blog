import Button from "@material-ui/core/Button";

export default function SignIn({ width }) {
  return (
    <Button href="/sign-in" variant="contained" color="primary">
      SignIn
    </Button>
  );
}
